name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e "core[dev]" -e "bots/coder[dev]" -e "bots/hr[dev]" pytest-cov
      - name: Test + coverage gate
        run: |
          pytest core/tests bots/coder/tests bots/hr/tests \
            --cov --cov-report=term-missing --cov-fail-under=85

  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e "core[dev]" -e "bots/coder[dev]" -e "bots/hr[dev]" ruff mypy
      - name: Lint
        run: ruff check core bots
      - name: Type-check
        run: mypy core bots

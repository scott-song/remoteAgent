# Example agent configuration
# Copy this file and customize for your project.
#
# Each agent maps to:
#   - One project directory (project_dir)
#   - Optionally one or more Feishu group chats (feishu_chat_ids)
#   - Claude model + permissions + security settings

name: example-project
display_name: "Example Project"
description: "Example agent for testing"

# Project directory — where Claude Code will operate
project_dir: /tmp/example-project

# Claude model (default: claude-opus-4-6)
# Options: claude-opus-4-6, claude-sonnet-4-5-20250929, claude-haiku-4-5-20251001
model: claude-opus-4-6

# Permission mode: plan | default | acceptEdits
permission_mode: acceptEdits

# System prompt (optional — Claude Code default is used if omitted)
# system_prompt: "You are an expert developer."

# Setting sources — enables CLAUDE.md and skills loading from the project
setting_sources: [user, project]

# Security
restricted: true                     # lock file access to project_dir
allowed_commands:                    # additional commands beyond the base set
  - npm
  - node
  - npx

# Feishu group chat binding (optional)
# Messages from these chats auto-route to this agent.
# Get chat_id from Feishu group info or from bot logs when a message arrives.
# feishu_chat_ids:
#   - oc_xxxxxxxxxxxxxxxx

# MCP servers (optional — merged with project's ~/.claude.json config)
# mcp_servers:
#   playwright:
#     command: npx
#     args: ["@anthropic/playwright-mcp-server"]

# Traceability matrix: platform/hardening

> Generated by `/sdlc-trace` · Last generated: 2026-06-12 · Source: spec `249915b`, plan `90f8c84`
> **Generated artifact — do not hand-edit. Regenerate with `/sdlc-trace platform/hardening`.**

## Summary

- ACs total: **10**
- Fully covered (int + e2e, or `n/a` justified): **0**
- Partially covered: **0**
- No coverage at any layer: **10**
- Open defects: **0** (no defects dir)
- Test report: **none yet** (testing stage not run)

> **Scope note.** This is a build/tooling/docs feature. Most ACs are verified by executing tooling (CI run, `make` targets, pre-commit) rather than by `int_AC_*` / `e2e_AC_*` unit tests — the kind of verification the Tester performs and records under *Non-functional ACs* in the test report. The matrix below reflects the **strict naming-convention grep only**; the absence of `int_AC_*`/`e2e_AC_*` tests is expected for infra ACs and is not by itself a defect. The unit suite (279 tests, 87.48% coverage) exists but does not use the AC-named convention, so it is invisible to this matrix by contract.

## AC → test matrix (strict convention only)

| AC | Description | Plan task(s) | Integration (`int_AC_n_*`) | E2E (`e2e_AC_n_*`) | Status |
|----|-------------|--------------|----------------------------|--------------------|--------|
| AC-1 | CI runs suite on Python 3.10/3.11/3.12, fails on any failure | T8 | — | — | ❌ |
| AC-2 | CI enforces 85% coverage floor | T8 | — | — | ❌ |
| AC-3 | `make lint` checks core/+bots/, exit 0/non-zero | T2, T3, T5 | — | — | ❌ |
| AC-4 | `make typecheck` checks core/+bots/, exit 0/non-zero | T2, T3, T5 | — | — | ❌ |
| AC-5 | pre-commit blocks commits with lint/format violations | T6 | — | — | ❌ |
| AC-6 | Reproducible deps via lockfile; `remoteagent-core` pinned | T1 | — | — | ❌ |
| AC-7 | Setup works on any Python ≥3.10; clear failure otherwise | T2 | — | — | ❌ |
| AC-8 | Operational output via stdlib logging; LOG_LEVEL; no bare print() in core/+bots/coder | T4 | — | — | ❌ |
| AC-9 | `.env.example` documents every read var, nothing extra | T7 | — | — | ❌ |
| AC-10 | CONTRIBUTING.md, ARCHITECTURE.md, .claude/CLAUDE.md exist & non-empty | T9 | — | — | ❌ |

## Gaps

- **No tests use the strict `int_AC_<n>_*` / `e2e_AC_<n>_*` naming convention** for any AC. The 279-test suite covers the code (87.48%) but is invisible to traceability.
- **AC-8 (LOG_LEVEL fallback)** — no test asserts that an unrecognized `LOG_LEVEL` falls back to INFO; `core/src/core/logging_config.py` has no dedicated test module.
- All 10 ACs require Tester verification (CI execution, `make` target runs, pre-commit, lockfile install) recorded under *Non-functional ACs* in the test report, OR AC-named integration tests for the testable ones (AC-8 logging behavior, AC-9 env-sync drift check).

## Non-functional ACs

*(none recorded — no test report yet. The Tester should classify AC-1/2/3/4/5/6/7/10 as `n/a — verified via <tooling execution>` and write AC-named tests for the programmatically-assertable parts of AC-8 and AC-9.)*

## Bugs

*(none — no defect records for this feature)*
